# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  - main

pool:
  vmImage: 'windows-latest'

steps:
- task: katalonTask@1
  displayName: 'Execute Katalon Studio project'
  inputs:
    version: '9.7.2'
    executeArgs: '-browserType=Chrome -retry=0 -statusDelay=15 -testSuitePath=Test Suites/OrangeHRMTestSuite -apiKey=$(2/1208463862626728/1208464105863601:1e26c37a1c220aa277c81098a7214973)'
